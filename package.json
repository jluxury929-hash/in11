{
  "name": "massive-trading-engine",
  "version": "1.0.0",
  "description": "Production-ready MEV Bot and Trading Engine",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "prebuild": "npm run clean",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mev",
    "flashbots",
    "ethereum",
    "trading"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.5",
    "dotenv": "^16.3.1",
    "ethers": "^5.7.2",
    "typescript": "^5.3.3"
    // NOTE: FlashbotsMEVExecutor dependencies (e.g., @flashbots/ethers-provider-bundle) 
    // are assumed to be handled by your FlashbotsMEVExecutor file.
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "ts-node": "^10.9.2"
  }
}
