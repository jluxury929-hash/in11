// config.ts
// Configuration loading utility. Most simple projects just use dotenv directly.

import * as dotenv from 'dotenv';
dotenv.config();

// The main bot config object definition (for export to other files)
// For simplicity, its definition is included in types.ts now.
